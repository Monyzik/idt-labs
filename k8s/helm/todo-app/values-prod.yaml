todo:
  replicaCount: 3
  resources:
    requests:
      cpu: 100m
      memory: 128Mi
    limits:
      cpu: 300m
      memory: 256Mi

ingress:
  hosts:
    - host: todo.prod.local
      paths:
        - path: /
          pathType: Prefix
